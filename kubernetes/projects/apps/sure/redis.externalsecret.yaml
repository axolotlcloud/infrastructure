apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: sure-redis-secret
  namespace: sure
spec:
  refreshInterval: 1h0m0s
  secretStoreRef:
    kind: SecretStore
    name: k8s-store
  target:
    name: sure-redis-secret
    template:
      data:
        REDIS_URL: "redis://:{{ .password }}@redis.sure.svc.cluster.local:6379/0"
  data:
  - secretKey: password
    remoteRef:
      key: redis-secret
      property: password
