apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- ./deployment.yaml
- ./sealedsecret.yaml

configMapGenerator:
- name: gate-config
  namespace: minecraft
  files:
  - files/config.yaml
