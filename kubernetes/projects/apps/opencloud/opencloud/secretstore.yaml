apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: k8s-store
  namespace: opencloud
spec:
  provider:
    kubernetes:
      remoteNamespace: opencloud
      server:
        caProvider:
          type: ConfigMap
          name: kube-root-ca.crt
          key: ca.crt
      auth:
        serviceAccount:
          name: eso-service-account
