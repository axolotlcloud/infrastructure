apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: collaboration
  namespace: opencloud
spec:
  parentRefs:
  - name: axolotl
    namespace: istio-ingress
  - name: axolotl-lan
    namespace: istio-ingress
  hostnames:
  - office-wopiserver.axolotl.cloud
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: collaboration
      port: 80
