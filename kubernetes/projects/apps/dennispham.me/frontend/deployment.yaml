apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: dennispham-me
spec:
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend
        image: ghcr.io/den4200/portfolio_frontend
        imagePullPolicy: Always
        resources:
          requests:
            cpu: 25m
            memory: 25Mi
          limits:
            cpu: 50m
            memory: 50Mi
        ports:
        - containerPort: 80
